---
description: Sponsor API integration and resilient API behavior
globs: api/**/*.ts,src/lib/**/*.ts,src/hooks/**/*.ts
alwaysApply: false
---

# API Integration Rule

See `api/CLAUDE.md` for the handler contract, secret handling, and the
per-integration rules for Gemini, ElevenLabs, Tiger Data, and buddy matching.
